import { ProgramaFidelidade } from './programa-fidelidade';

describe('ProgramaFidelidade', () => {
  it('should create an instance', () => {
    expect(new ProgramaFidelidade()).toBeTruthy();
  });
});
