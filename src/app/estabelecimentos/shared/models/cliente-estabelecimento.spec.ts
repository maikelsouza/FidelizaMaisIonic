import { ClienteEstabelecimento } from './cliente-estabelecimento';

describe('ClienteEstabelecimento', () => {
  it('should create an instance', () => {
    expect(new ClienteEstabelecimento()).toBeTruthy();
  });
});
