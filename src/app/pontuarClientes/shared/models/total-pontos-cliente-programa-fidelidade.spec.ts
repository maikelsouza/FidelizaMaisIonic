import { TotalPontosClienteProgramaFidelidade } from './total-pontos-cliente-programa-fidelidade';

describe('TotalPontosClienteProgramaFidelidade', () => {
  it('should create an instance', () => {
    expect(new TotalPontosClienteProgramaFidelidade()).toBeTruthy();
  });
});
