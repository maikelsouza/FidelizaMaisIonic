import { TipoEstabelecimento } from './tipo-estabelecimento';

describe('TipoEstabelecimento', () => {
  it('should create an instance', () => {
    expect(new TipoEstabelecimento()).toBeTruthy();
  });
});
