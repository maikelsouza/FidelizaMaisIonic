import { CampoRegistroCartaoFidelidade } from './campo-registro-cartao-fidelidade';

describe('CampoRegistroCartaoFidelidade', () => {
  it('should create an instance', () => {
    expect(new CampoRegistroCartaoFidelidade()).toBeTruthy();
  });
});
