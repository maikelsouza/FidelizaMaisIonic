import { CampoItemProgramaFidelidade } from './campo-item-programa-fidelidade';

describe('CampoItemProgramaFidelidade', () => {
  it('should create an instance', () => {
    expect(new CampoItemProgramaFidelidade()).toBeTruthy();
  });
});
