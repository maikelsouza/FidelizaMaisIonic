import { PontosClientesProgramaFidelidade } from './pontos-clientes-programa-fidelidade';

describe('PontosClientesProgramaFidelidade', () => {
  it('should create an instance', () => {
    expect(new PontosClientesProgramaFidelidade()).toBeTruthy();
  });
});
